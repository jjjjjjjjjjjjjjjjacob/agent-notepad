# Security hardening verification log

The baseline audit is security-audit-2026-09-05.md. This log records follow-up work; it does not claim fixes are deployed. Root main remains e1dad6e with the user's concurrent UI changes preserved.

## Reviewed changes
- Vouch: c568722 in /tmp/agent-notepad-vouch-20260906, 14 tests passed; activation pending hosted main/rules.
- WorkOS: 366b6f2 in /tmp/agent-notepad-security-workos-20260906, primary full-diff review and independent typecheck/lint/23 tests/diff checks passed. Bounds expensive authentication across requests and reuses SDK key cache while retaining live authorization.
- Moderation boundaries:3978957 in /tmp/agent-notepad-security-moderation-20260906. Primary full17-file diff and test review, independent typecheck/lint/85 tests/diff checks passed. Separate audience evidence records preserve large forensic originals without leaking them to reporters; derived tasks retain both report and title revision provenance.
- Sandbox wallet:7dffea2 in /tmp/agent-notepad-security-place-20260906. Primary reviewed all8 files including new capacity/retry regressions, and independently reran typecheck, lint,69 Place/integrity tests (including the large ownership fixture) and diff checks successfully. Atomic reservations, exact legacy inventory, bounded retries, durable parked obligations and operator recovery are implemented locally.
- Public visibility:bd1ff24 in /tmp/agent-notepad-security-visibility-20260906 includes checkpoint3ac78d3 and reviewed002 cherry-pick edabb26. Primary reviewed the complete checkpoint, dependency conflict resolution and two-file continuation, then independently reran typecheck, lint,117 tests across7 files and diff checks. Explicit committee guards and lightweight provenance checks preserve graph limits; real fallback task-title marker assertion now passes.

## Hosted controls
- September6: enabled vulnerability alerts; GET verified204, alert list currently empty.
- September6: enabled Actions full SHA pinning; GET verified true. Existing read-only default token and disabled workflow PR approval preserved.
- Visibility remains private; sole human writer remains Jacob. Branch protection still requires public visibility or eligible plan. No publication, deployment, issue/PR creation or main-branch merge occurred.
- Current GitHub documentation used for supported API fields: https://docs.github.com/en/rest/actions/permissions and https://docs.github.com/en/rest/repos/repos .
- September6 hosted Convex inspection: names-only CLI queries for production gregarious-chickadee-782 and development incredible-boar-27 each returned only BETTER_AUTH_SECRET, SITE_URL and TRUSTED_ORIGINS. Secret values were never output; a private in-memory comparison verified distinct secrets, both at least32 characters and neither matching the isolated test placeholder. Production origins are explicit HTTPS Vercel domains without wildcards/userinfo. Development additionally permits local3843 and the owner's Vercel preview wildcard.
- No MODERATION_ENABLED, moderation gateway/classifier/admin configuration, WorkOS, Stripe or Place variables were present on either named deployment. These optional integrations/enforcement paths are not established as active by deployed environment. Do not enable them without their provider/administrator setup and acceptance tests. No deployed variable was changed.

## Verification and review discoveries
- Gitleaks8.30.1 all-reference history scan after new commits:9 commits, ~2.07MB, no findings. Redaction enabled; ignore directives disabled.
- Current root Bun dependency audit, including concurrent UI package additions: no advisories returned.
- Plan003 checkpoint3ac78d3 has graph/profile visibility and bounded reads;36 targeted tests passed in executor, not yet final approval. Primary read complete diff/tests. It awaits plan002's lightweight task predicate and the related-task takedown assertion.
- Graph regression originally reproduced four withdrawal/profile surfaces. Additional stress fixture showed4201 read operations; budgeted implementation stays below4096. Actual transaction-limit fixtures exercise40 contract-valid ~540KB revisions,40 gap sources and80 related tasks. All views now need shared cached/bounded reads to avoid introducing denial of service through visibility checking.
- Primary review discovered knowledge-gap task title provenance missing during safe integrity fallback; added to002 instead of hiding the failing expectation.
- Primary review identified full snapshot plus duplicate subject view could exceed1MiB; executor reproduced1,376,499 bytes with a valid590–600KB publication.002 now separates audience rows and avoids repeatedly wrapping inherited forensic content.
- Primary review identified pre-existing full-history authorship scans in moderation case creation and jury eligibility.008 tracks a bounded fix; source-confirmed and awaiting capacity regression.

## Remaining work
All nine local plans are approved. The final integration branch includes every reviewed fix, five cross-plan regressions, staged-index scanning and the embedding archive-bypass guard. Local verification and full external-review triage are complete. Account/provider deployment controls remain separately unverified where current credentials cannot inspect them. Hosted activation remains distinct from local implementation.

## Review update — bounded governance and integration

Final external review at967d16d is running. Its first lead concerns SHA-scoped Vouch commit statuses. Primary verified the code's all-open-PR same-head conservative decision and before/after canonical checks. A status does not grant write/merge authority; sole-writer permissions remain the boundary. The underlying same-SHA/new-PR event race is real and cannot be made atomic through the status API. It is recorded as residual gate staleness requiring Jacob's author/trust verification, not a demonstrated outsider write bypass. Removing the gate as proposed would not strengthen the existing controls. No change is made solely from reviewer instructions.

Plan008 approved at9f65305: all ten implementation/documentation/test files reviewed; primary typecheck/lint and82 moderation tests across four files passed, including18 new real-limit regressions. New limits deliberately withhold decisions on incomplete attribution; they do not invent historical ownership or authorize uncertain jurors. Integration009 may now intake this exact commit, retaining005's retirement guard before008's inherited-evidence preload. No deployment occurred.

Plan005 approved at1f462d6 after the primary read all changed implementation and tests. Independent typecheck/lint passed. The first49-test run had48 passes and one20.135-second capacity-fixture timeout during competing container work; that exact test passed in12.39 seconds in isolation. The42 governance/moderation tests then passed together in14.54 seconds, and seven screening tests passed separately. Original timeouts and fixtures were retained. Integration009 starts from the reviewed003 branch containing002, then adds001/004/005/006 and approved008.007 remains gated on primary approval.

The normal local test ports were occupied by user development processes.009 may use a separate disposable verification checkout with only exact test port literals changed; no production/development validation logic or integration source is changed. Initial integration lint found a CommonJS annotation missing in001's test; the already-reviewed007 checkpoint supplies that exact file-local annotation, so no duplicate fix is added.

Verification incident: an executor process-inventory command printed the generated credential for its own disposable local Convex instance in a tool response. The value is not repeated here. The instance was stopped and all alternate ports were free. The primary authorized deleting only `/tmp/agent-notepad-security-e2e-20260906/.artifacts/test-backend` and any owned captured process-list artifact, then generating a new local instance. Shared/main/hosted credentials must not be touched. Existing tool transcript text cannot be erased; retiring the disposable instance is the containment. Subsequent process inspection is limited to PID/command-name/cwd metadata. Final containment verification is pending.

Containment verified by the executor: all three owned backend PIDs absent,3315/3316/3317/4342 free, only the disposable `.artifacts/test-backend` directory removed. There was no captured process-list file to remove. Main/shared/hosted configuration was untouched; any next run must generate a fresh local instance.

Plan007 approved at030d7c2 after primary full25-file review and independent verification:209 application tests/one existing skip,27 Node tests,typecheck/lint/actionlint,signature verification,secret/dependency scans,live rejection/redaction fixtures,exact36-package/hash reproduction,actual final image gate and hardened service/provenance tests. The final image retains21 temporarily accepted medium/low Debian findings expiring2026-10-06; no Python/high/critical findings were reported. The primary's cached rebuild had the same filesystem layer digests as the tested image. Integration intake is authorized.

007 runtime proof removed the slim image's high/critical findings and vulnerable pip tooling while preserving the real model and36 locked packages. The proposed image retains21 unfixed Debian medium/low advisories, with an explicit exact-version baseline expiring2026-10-06. Primary reviewed this residual-risk decision and matched all ten platform-tool SHA256 values to official GitHub release asset digests. Final image/code verification is pending; no claim of zero vulnerabilities.

## Final verification update

Primary final application source `967d16de385f888f71234039beaedf5d43045195`: typecheck/lint,312 application tests with one existing external integration skip,27 Node tests,redacted history/current scans and Bun/Python dependency gates passed. Independent E2E passed24/24 in1.4 minutes against a newly generated disposable local backend. Independent optimized build compiled successfully, passed TypeScript and generated18/18 static pages. Only the previously reviewed31 exact test port substitutions in12 files were used in the disposable checkout; integration source was not changed. Both owned frontend/backend services stopped;3315/3316/3317/4342 closed. Fresh disposable backend configuration/data were deleted after verification; logs remain ignored.

The external review confirmed a local staged-index coverage gap: cached filenames were scanned using working-copy bytes. Plan009 now authorizes a separate validated Git-index snapshot and live staged-token regression. The suggestion to reject all unused, unexpired exact image exceptions was rejected: those entries cannot match a different package/version/CVE/distro or exceed their review lifetime, so no widening of accepted findings was established. Expired/malformed unused entries already fail. Final reviewer output remains pending.

A further external-review suggestion requests a structured NOT_FOUND error instead of a non-null assertion for a missing resource revision in `convex/moderation/cases.ts`. Primary read the lookup and transaction flow: the absent-reference case already throws before any writes, and no unauthorized data or authority is returned. This is diagnostic cleanup, not an additional demonstrated security defect; no source expansion was authorized.

Final scanner correction approved locally at `cf08ec33ed185d2f2e32510a3b3aa2b443077df3`. Primary read all four changed files and meaningful tests, confirmed exact scope/clean worktree, and independently passed all31 Node/Vouch tests, lint, actual all-ref history/index/worktree scanning, and actual live fixtures for staged overwritten/deleted canaries plus clean repository and known-vulnerable dependency. The executor also confirmed the new live fixture fails against the prior scanner. No application/image/dependency source changed. A narrow external rereview of only this four-file correction has started; the full external review is still completing.

Full CodeRabbit review completed successfully at application source967d16d:5 suggestions. The fifth lead was FastEmbed0.8.0's unfiltered archive helper. Primary verified official tag source and open upstream issue626, then checked the exact hardened image: source contains the unsafe helper, but the service always uses its own pinned HF revision and specific_model_path early-return; real normalized384-vector generation succeeds with all four fallback/archive helpers guarded and never called; empty local cache raises LocalEntryNotFoundError without fallback. Integration executor is adding only CI regression/README documentation. No patched published release was verified and no production monkeypatch or package upgrade is justified by this service path.

The attempted narrow scanner external rereview exited with the free CLI rate limit (reported9-minute wait); no paid action, plugin install, repository publication or additional permissions were used. This external verification limit does not erase the independent full-diff review and actual scanner fixtures.

Final hosted read-only recheck: repository PRIVATE with Issues enabled; Jacob is the sole listed collaborator/admin writer; Actions SHA pinning remains required, default token read-only and workflow PR review approval disabled; vulnerability alerts remain enabled. Main and origin/main are still exactly e1dad6e. No publication, push, merge or deployment occurred.

Embedding guard independently passed in the primary updated runner at2c9b309: offline service startup, exact package/file provenance, real vectors/HTTP validation, no pip, forbidden archive/fallback helpers and missing-cache refusal. Primary requested one test-input refinement from unsupported `document` to the service's actual `passage` input kind; no production behavior is involved. The exact final guard run will be recorded after that correction.

## Final verdict — September6

APPROVED at `21725f66b60f6912fbca0e18a176c2c2be9ee06b`, clean `codex/security-hardening` worktree. Primary independently passed the final updated image runner,31 Node/Vouch tests,lint and full diff checks after the `passage` refinement; final all-ref history/index/current secret scan also passed. Only seven CI/test/doc files differ after the application source967d16d used for312 passing application tests,24 passing E2E tests and the successful18-page optimized build. Full CodeRabbit review completed; narrow repeat was rate-limited and is recorded as a verification limit. No paid capacity used.

All owned agent-notepad-test containers are absent,3315/3316/3317/4342 closed, disposable backend directory absent. Main/origin remain e1dad6e, concurrent user source changes preserved. Final report: security-review-2026-09-06.md. Hosted activation is still required; no claim of bulletproof software or deployed enforcement.
